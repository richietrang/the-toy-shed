import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const ProfileLoginScreen = () => {
    return (
        <View>
            <Text>Profile Login Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default ProfileLoginScreen;

