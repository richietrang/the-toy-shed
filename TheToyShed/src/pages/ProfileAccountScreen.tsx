import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const ProfileAccountScreen = () => {
    return (
        <View>
            <Text>Profile Account Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default ProfileAccountScreen;

