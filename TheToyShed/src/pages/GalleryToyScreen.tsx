import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const GalleryToyScreen = () => {
    return (
        <View>
            <Text>Gallery Toy Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default GalleryToyScreen;

