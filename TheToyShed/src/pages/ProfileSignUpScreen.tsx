import React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const ProfileSignUp = () => {
    return (
        <View>
            <Text>Profile Sign Up Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default ProfileSignUp;

